/**
 * No-op semantic slot reserved for fallback implementations that preserve starter contracts
 * without enabling a real delivery channel.
 *
 * <p>The current module keeps behavior unchanged and therefore does not register a no-op
 * implementation by default.</p>
 */
package com.velox.email.noop;
