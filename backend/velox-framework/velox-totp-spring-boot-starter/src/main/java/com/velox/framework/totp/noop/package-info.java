/**
 * No-op semantic slot reserved for fallback implementations that preserve starter contracts
 * without enabling the real TOTP capability. Used when {@code velox.totp.enabled=false}.
 */
package com.velox.framework.totp.noop;
